Release checklist for `impl-07-dependent-closure-and-release-hardening`

- Verified on 2026-04-10 from current `HEAD`.
- Final upstream suite status: pass. `cmake -S safe/tests/upstream -B /tmp/libuv-safe-final-tests -DLIBUV_SAFE_STAGE=/tmp/libuv-safe-stage`, `cmake --build /tmp/libuv-safe-final-tests -j"$(nproc)"`, and `RES_OPTIONS=attempts:0 UV_TEST_TIMEOUT_MULTIPLIER=2 ctest --test-dir /tmp/libuv-safe-final-tests --output-on-failure` passed. Inventory parity with `original/build-checker-review` remained `440 / 440 / 55`.
- Relink matrix status: pass. `safe/tools/relink_original_tests.sh --shared --run` and `--static --run` passed for `original/build-checker`, `original/build-checker-review`, `original/build-checker-verify`, and `original/build-checker-audit`.
- Package payload contract: pass. `safe/tools/build_deb.sh`, `safe/tools/stage_install.sh`, `safe/tools/verify_stage_install.sh`, `safe/tools/verify_runner_inventory.sh`, `safe/tools/verify_exports.sh`, `safe/tools/verify_sizes.sh`, and `python3 safe/tools/verify_deb_payload_contract.py safe/dist/artifacts.env original/include safe/debian/not-installed` passed. The staged include tree matched `original/include`, and the packaged shared library satisfied 307 exported symbols plus 307 Debian non-optional symbols.
- 16-dependent image status: pass. `safe/tools/build_dependents_image.sh`, `safe/tools/verify_dependents_image_contract.sh`, and the exact `safe/tools/run_dependents_suite.sh --image libuv-safe-dependents:check-14 --mode full --assert-packaged-libuv` witness passed against the packaged image, including three consecutive reruns after the Knot Resolver stabilization. The gevent probe now refreshes APT metadata before `apt-get source` and leaves per-run logs behind on failure, and the Knot Resolver probe now uses a local forged answer instead of outbound recursion so the full matrix remains deterministic.
- Packaged compile-smoke status for `libuv.pc` and `libuv-static.pc`: pass. A fresh Ubuntu 24.04 container installed the runtime and development `.deb` packages, `pkg-config --exists libuv` and `pkg-config --exists libuv-static` succeeded, and both shared and static `uv_version()` smoke programs compiled and ran.
- Wrapper status: pass. `test-original.sh` passed in original-mode with `LIBUV_TEST_ORIGINAL_IMAGE=libuv-safe-dependents:check-14`, and it also passed in safe-package mode with `LIBUV_SAFE_DEB_DIR=safe/dist LIBUV_TEST_ORIGINAL_IMAGE=libuv-safe-dependents:check-14`. Both modes consumed the same checked-in probe assets.
- Privileged spawn or security checks: pass. A root-gated Ubuntu 24.04 container rebuilt the focused harness with `LIBUV_SAFE_NO_PYTHON=1` and passed `spawn_setuid_setgid`, `spawn_setuid_fails`, and `spawn_setgid_fails`. `safe/src/unix/process.rs` still applies credential dropping in `setgroups`, then `setgid`, then `setuid` order.
- Unsafe audit status: pass. `python3 safe/tools/audit_unsafe.py safe/src` passed, `rg -n "unsafe fn" safe/src` returned no matches, and `safe/src/unix/epoll.rs` keeps `uv__kernel_version()` plus `uv__use_io_uring()` as return-0 stubs for this release.
- Regression and dependent probes: pass. `safe/tools/run_regressions.sh --stage /tmp/libuv-safe-stage --up-to-phase impl-07-dependent-closure-and-release-hardening` passed. The dependent manifest was updated with the gevent source-build plus runtime coverage that caught the package-image failure mode.
- `all_cves.json` review status: pass. `CVE-2015-0278`, `CVE-2021-22918`, `CVE-2024-22017`, and `CVE-2024-24806` are all present in `confirmed_cve_ids`.
- Known intentional choices: the Linux runtime stays epoll-only for this release, `io_uring` remains intentionally disabled at runtime, the focused-harness fallback inputs under `safe/tests/harness/**` stay frozen, and the workflow does not use a clean `git status` as a release gate.
