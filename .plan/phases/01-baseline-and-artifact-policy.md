# Baseline Contract, Artifact Policy, and Verifier Normalization

Phase Name: Baseline Contract, Artifact Policy, and Verifier Normalization

Implement Phase ID: `impl-01-baseline-and-artifact-policy`

## Preexisting Inputs
- `/home/yans/safelibs/port-libuv/.cargo/config.toml`
- `/home/yans/safelibs/port-libuv/safe/Cargo.toml`
- `/home/yans/safelibs/port-libuv/safe/build.rs`
- `/home/yans/safelibs/port-libuv/safe/src/**`
- `/home/yans/safelibs/port-libuv/safe/include/**`
- `/home/yans/safelibs/port-libuv/safe/tools/abi-baseline.json`
- `/home/yans/safelibs/port-libuv/safe/tools/cc-linker.sh`
- `/home/yans/safelibs/port-libuv/safe/tools/stage_install.sh`
- `/home/yans/safelibs/port-libuv/safe/tools/build_upstream_harness.sh`
- `/home/yans/safelibs/port-libuv/safe/tools/run_upstream_tests.sh`
- `/home/yans/safelibs/port-libuv/safe/tools/verify_exports.sh`
- `/home/yans/safelibs/port-libuv/safe/tools/verify_sizes.sh`
- `/home/yans/safelibs/port-libuv/safe/tests/harness/**`
- `/home/yans/safelibs/port-libuv/safe/tests/upstream/**`
- `/home/yans/safelibs/port-libuv/safe/tests/regressions/**`
- `/home/yans/safelibs/port-libuv/original/include/**`
- `/home/yans/safelibs/port-libuv/original/debian/**`
- `/home/yans/safelibs/port-libuv/original/libuv.pc.in`
- `/home/yans/safelibs/port-libuv/original/libuv-static.pc.in`
- `/home/yans/safelibs/port-libuv/original/test/**`
- `/home/yans/safelibs/port-libuv/original/build-checker/**`
- `/home/yans/safelibs/port-libuv/original/build-checker-review/**`
- `/home/yans/safelibs/port-libuv/original/build-checker-verify/**`
- `/home/yans/safelibs/port-libuv/original/build-checker-audit/**`

## New Outputs
- `/home/yans/safelibs/port-libuv/.plan/workflow-run-base.txt`
- `/home/yans/safelibs/port-libuv/.gitignore`
- `/home/yans/safelibs/port-libuv/safe/.gitignore`
- updated `/home/yans/safelibs/port-libuv/original/.gitignore`
- `/home/yans/safelibs/port-libuv/safe/tools/verify_phase_head.sh`
- `/home/yans/safelibs/port-libuv/safe/tools/verify_phase_commit_sequence.py`
- `/home/yans/safelibs/port-libuv/safe/tools/verify_stage_install.sh`
- `/home/yans/safelibs/port-libuv/safe/tools/verify_runner_inventory.sh`
- `/home/yans/safelibs/port-libuv/safe/tools/run_regressions.sh`
- updated `/home/yans/safelibs/port-libuv/safe/tests/harness/README.md`
- updated `/home/yans/safelibs/port-libuv/safe/tests/upstream/README.md`
- `/home/yans/safelibs/port-libuv/safe/tests/regressions/manifest.json`
- optional `/home/yans/safelibs/port-libuv/safe/tests/regressions/README.md`
- updated `/home/yans/safelibs/port-libuv/safe/COMPATIBILITY_NOTES.md`

## File Changes
- Record the current workflow-run base so final commit-sequence validation is bounded to this workflow run instead of older branch history.
- Create ignore rules for generated-but-authoritative artifact paths.
- Add reusable verification helpers for staged installs, runner inventories, and phase-commit validation.
- Refresh docs to describe the real artifact flow and the no-repo-wide-clean-tree rule.

## Implementation Details
- Consume the existing checked-in and prebuilt artifacts in place. Do not rediscover or regenerate alternatives for `original/build-checker/**`, `original/build-checker-review/**`, `original/build-checker-verify/**`, `original/build-checker-audit/**`, `original/include/**`, `safe/tests/harness/**`, or `safe/tests/upstream/**`.
- Create `/home/yans/safelibs/port-libuv/.plan/workflow-run-base.txt` in the `impl-01-baseline-and-artifact-policy` commit, with the file body set to the exact commit hash at `HEAD` immediately before that phase creates its first workflow commit. If phase 1 is bounced later, preserve the same recorded base hash rather than moving the run boundary forward.
- Create a repo-root `.gitignore` for repo-root package side artifacts generated by `dpkg-buildpackage` and any other root-level packaging scratch outputs.
- Create `safe/.gitignore` for `target/` and `dist/`.
- Extend `original/.gitignore` for `.safe-relink/**` beneath the four authoritative build trees plus local debug scratch such as `.gdb_history`; do not ignore, delete, or recreate the `build-checker*` trees themselves.
- Implement `verify_phase_head.sh` so it fails unless `git log -1 --format='%s'` begins with the requested implement phase ID.
- Implement `verify_phase_commit_sequence.py` with the CLI `--base-file <path> <phase-id>...`. It must read the base commit hash from the supplied file, walk `git log --first-parent --format='%H %s' <base>..HEAD`, require every commit subject in that range to begin with one of the supplied phase IDs, require the first commit in range to begin with the first supplied phase ID, require `HEAD` to begin with the last supplied phase ID, and fail unless the phase order across the entire range is monotonic and every supplied phase ID appears at least once.
- Implement `verify_stage_install.sh` so it checks:
  - `libuv.so -> libuv.so.1 -> libuv.so.1.0.0`
  - `libuv.a`
  - `lib/pkgconfig/libuv.pc`
  - `lib/pkgconfig/libuv-static.pc`
  - a byte-for-byte comparison between `<stage>/include/**` and `original/include/**`, covering `include/uv.h` plus every file under `original/include/uv/`
  - a trivial dynamic C client builds and runs with `PKG_CONFIG_PATH=<stage>/lib/pkgconfig pkg-config --cflags --libs libuv`
  - a trivial static C client builds and runs with `PKG_CONFIG_PATH=<stage>/lib/pkgconfig pkg-config --cflags --libs --static libuv-static`
- Implement `verify_runner_inventory.sh` so it reads the four prebuilt upstream runner trees and asserts the existing `435 / 435 / 55` vs `440 / 440 / 55` split instead of inventing new counts.
- Create `safe/tests/regressions/manifest.json` and `safe/tools/run_regressions.sh` as the only executable contract for `safe/tests/regressions/**`.
- `safe/tests/regressions/manifest.json` must use the exact schema `{"schema_version": 1, "regressions": [...]}` and every entry must contain exactly `id`, `path`, `runner`, `phase_owner`, `compile_flags`, `link_flags`, and `args`.
- The initial regression manifest must register the existing `fs_readlink_proc_self.c` and `getaddrinfo_long_hostname.c` files with stable IDs, slash-free `path` values, `runner: c`, `phase_owner: impl-03-io-fs-resolver-parity`, and explicit empty arrays for unused manifest fields.
- `run_regressions.sh` must accept `--stage <prefix> --up-to-phase <impl-id>`, validate the manifest schema against the flat files actually present in `safe/tests/regressions/`, compile manifest-declared `c` regressions with `cc`, compile `cpp` regressions with `c++`, execute `shell` regressions with `bash`, and treat an empty filtered set as success.
- Refresh `safe/tests/harness/README.md` to say that `safe/tools/build_upstream_harness.sh` owns the focused selected-test inventory and that the checked-in `safe/tests/harness/phase-test-list.h` plus `safe/tests/harness/uv-safe-run-tests.c` are fallback templates for `LIBUV_SAFE_NO_PYTHON=1`.
- Keep `safe/tests/upstream/**` in place. Only update build glue or documentation if the helper scripts need a clearer contract.
- Make it explicit that final reviewers validate `HEAD` and first-parent commit subjects, not a clean worktree.

## Verification Phases
### `check-01-baseline-contract`
- Phase ID: `check-01-baseline-contract`
- Type: `check`
- Fixed `bounce_target`: `impl-01-baseline-and-artifact-policy`
- Purpose: prove that the current Rust library still builds, stages, exports, and sizes correctly before runtime parity work starts.
- Commands:

```bash
/home/yans/safelibs/port-libuv/safe/tools/verify_phase_head.sh impl-01-baseline-and-artifact-policy
test -f /home/yans/safelibs/port-libuv/.plan/workflow-run-base.txt
base_commit="$(tr -d '\n' < /home/yans/safelibs/port-libuv/.plan/workflow-run-base.txt)" && git rev-parse --verify "${base_commit}^{commit}" >/dev/null && git merge-base --is-ancestor "$base_commit" HEAD
cargo build --manifest-path /home/yans/safelibs/port-libuv/safe/Cargo.toml --release
/home/yans/safelibs/port-libuv/safe/tools/stage_install.sh /tmp/libuv-safe-stage
/home/yans/safelibs/port-libuv/safe/tools/verify_stage_install.sh /tmp/libuv-safe-stage
diff -ruN /home/yans/safelibs/port-libuv/original/include /tmp/libuv-safe-stage/include
/home/yans/safelibs/port-libuv/safe/tools/verify_exports.sh /tmp/libuv-safe-stage/lib/libuv.so /home/yans/safelibs/port-libuv/original/build-checker/libuv.so.1.0.0 /home/yans/safelibs/port-libuv/original/debian/libuv1t64.symbols
/home/yans/safelibs/port-libuv/safe/tools/verify_sizes.sh /tmp/libuv-safe-stage
/home/yans/safelibs/port-libuv/safe/tools/verify_runner_inventory.sh
```

### `check-02-focused-harness-and-artifact-policy`
- Phase ID: `check-02-focused-harness-and-artifact-policy`
- Type: `check`
- Fixed `bounce_target`: `impl-01-baseline-and-artifact-policy`
- Purpose: verify focused-harness builds against the staged install in generator-backed and explicit no-Python fallback modes, keep fallback templates synchronized with the generator, and enforce the generated-artifact policy without repo-wide clean-tree assumptions.
- Commands:

```bash
/home/yans/safelibs/port-libuv/safe/tools/verify_phase_head.sh impl-01-baseline-and-artifact-policy
cargo build --manifest-path /home/yans/safelibs/port-libuv/safe/Cargo.toml --release
/home/yans/safelibs/port-libuv/safe/tools/stage_install.sh /tmp/libuv-safe-stage
command -v python3 >/dev/null
/home/yans/safelibs/port-libuv/safe/tools/build_upstream_harness.sh --stage /tmp/libuv-safe-stage --build /tmp/libuv-safe-focused
LIBUV_SAFE_NO_PYTHON=1 /home/yans/safelibs/port-libuv/safe/tools/build_upstream_harness.sh --stage /tmp/libuv-safe-stage --build /tmp/libuv-safe-focused-nopy
diff -u /tmp/libuv-safe-focused/generated/phase-test-list.h /tmp/libuv-safe-focused-nopy/generated/phase-test-list.h
diff -u /tmp/libuv-safe-focused/generated/uv-safe-run-tests.c /tmp/libuv-safe-focused-nopy/generated/uv-safe-run-tests.c
diff -u /tmp/libuv-safe-focused/generated/benchmark-sizes-main.c /tmp/libuv-safe-focused-nopy/generated/benchmark-sizes-main.c
RES_OPTIONS=attempts:0 UV_TEST_TIMEOUT_MULTIPLIER=2 /home/yans/safelibs/port-libuv/safe/tools/run_upstream_tests.sh --build /tmp/libuv-safe-focused --shared --tests version,run_once,async,threadpool_queue_work_simple,tcp_ping_pong,udp_send_and_recv,spawn_stdout,fs_file_async,getaddrinfo_basic
RES_OPTIONS=attempts:0 UV_TEST_TIMEOUT_MULTIPLIER=2 /home/yans/safelibs/port-libuv/safe/tools/run_upstream_tests.sh --build /tmp/libuv-safe-focused-nopy --shared --tests version,run_once,async,tcp_ping_pong,spawn_stdout,fs_file_async,getaddrinfo_basic
RES_OPTIONS=attempts:0 UV_TEST_TIMEOUT_MULTIPLIER=2 /home/yans/safelibs/port-libuv/safe/tools/run_upstream_tests.sh --build /tmp/libuv-safe-focused --static --tests version,run_once,tcp_ping_pong
RES_OPTIONS=attempts:0 UV_TEST_TIMEOUT_MULTIPLIER=2 /home/yans/safelibs/port-libuv/safe/tools/run_upstream_tests.sh --build /tmp/libuv-safe-focused-nopy --static --tests version,run_once,tcp_ping_pong
cmake -S /home/yans/safelibs/port-libuv/safe/tests/upstream -B /tmp/libuv-safe-upstream-lists -DLIBUV_SAFE_STAGE=/tmp/libuv-safe-stage
cmake --build /tmp/libuv-safe-upstream-lists -j"$(nproc)"
diff -u <(/tmp/libuv-safe-upstream-lists/uv_run_tests --list | sort) <(/home/yans/safelibs/port-libuv/original/build-checker-review/uv_run_tests --list | sort)
diff -u <(/tmp/libuv-safe-upstream-lists/uv_run_tests_a --list | sort) <(/home/yans/safelibs/port-libuv/original/build-checker-review/uv_run_tests_a --list | sort)
diff -u <(/tmp/libuv-safe-upstream-lists/uv_run_benchmarks_a --list | sort) <(/home/yans/safelibs/port-libuv/original/build-checker-review/uv_run_benchmarks_a --list | sort)
test "$(/tmp/libuv-safe-upstream-lists/uv_run_tests --list | awk 'END { print NR }')" = 440
test "$(/tmp/libuv-safe-upstream-lists/uv_run_tests_a --list | awk 'END { print NR }')" = 440
test "$(/tmp/libuv-safe-upstream-lists/uv_run_benchmarks_a --list | awk 'END { print NR }')" = 55
python3 - <<'PY'
import json
from pathlib import Path

regressions_root = Path('/home/yans/safelibs/port-libuv/safe/tests/regressions')
manifest = json.loads((regressions_root / 'manifest.json').read_text())
assert set(manifest) == {'schema_version', 'regressions'}, manifest
assert manifest['schema_version'] == 1
files = {
    path.name
    for path in regressions_root.iterdir()
    if path.is_file() and path.name not in {'manifest.json', 'README.md'}
}
expected_keys = {'id', 'path', 'runner', 'phase_owner', 'compile_flags', 'link_flags', 'args'}
manifest_files = set()
for item in manifest['regressions']:
    assert set(item) == expected_keys, item
    assert item['runner'] in {'c', 'cpp', 'shell'}
    assert isinstance(item['path'], str) and item['path'] and '/' not in item['path'], item['path']
    assert isinstance(item['compile_flags'], list), item
    assert isinstance(item['link_flags'], list), item
    assert isinstance(item['args'], list), item
    manifest_files.add(item['path'])
assert {'fs_readlink_proc_self.c', 'getaddrinfo_long_hostname.c'}.issubset(files)
assert manifest_files == files, (manifest_files, files)
PY
```

- Review checks:
  - Confirm the ignore policy covers `safe/target/**`, `safe/dist/**`, repo-root `*.buildinfo` / `*.changes` / `*.ddeb`, and `original/build-checker*/.safe-relink/**`.
  - Confirm the checker instructions do not use repo-wide `git status` gates.
  - Confirm `safe/tests/regressions/manifest.json` is the single checked-in inventory for executable regressions under `safe/tests/regressions/**`.
  - Confirm later containerized focused-harness invocations that omit `python3` set `LIBUV_SAFE_NO_PYTHON=1` explicitly instead of depending on Ubuntu image defaults.

## Success Criteria
- `check-01-baseline-contract` and `check-02-focused-harness-and-artifact-policy` both pass.
- `.plan/workflow-run-base.txt` records the pre-phase `HEAD` commit and remains unchanged on later bounces.
- The artifact-ignore policy and verifier helpers are in place without any repo-wide clean-tree assertion.
- `safe/tests/regressions/manifest.json` and `safe/tools/run_regressions.sh` establish the sole executable regression contract for `safe/tests/regressions/**`.
- The latest commit produced by this phase begins with `impl-01-baseline-and-artifact-policy`.

## Git Commit Requirement
The implementer must commit the phase work to git before yielding. The newest commit at `HEAD` must remain the handoff commit for this phase, and its subject must begin with `impl-01-baseline-and-artifact-policy`.
